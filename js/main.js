
const header=document.querySelector('header.site');const burger=document.querySelector('.burger');if(header&&burger){const setMenu=(open)=>{header.classList.toggle('open',open);burger.setAttribute('aria-expanded',String(open));burger.setAttribute('aria-label',open?'Fermer le menu':'Ouvrir le menu')};burger.addEventListener('click',()=>setMenu(!header.classList.contains('open')));document.addEventListener('keydown',e=>{if(e.key==='Escape')setMenu(false)});window.matchMedia('(min-width:981px)').addEventListener('change',()=>setMenu(false));}
const form=document.querySelector('form.contact-form');if(form){form.addEventListener('submit',e=>{e.preventDefault();form.style.display='none';const ok=document.querySelector('.form-success');if(ok)ok.style.display='block';});}
